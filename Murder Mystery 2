-- Cleanup previous execution
if getgenv().MM2Admin_Cleanup then
    getgenv().MM2Admin_Cleanup()
end

local success, StarsationLibrary = pcall(function()
	return loadstring(game:HttpGet("https://raw.githubusercontent.com/StarsationSetanya/hee/refs/heads/main/ui.lua"))()
end)

if not success or not StarsationLibrary then
	return
end

local plrs = game:GetService("Players")
local lplayer = plrs.LocalPlayer
local workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

-- Variables for logic
getgenv().MM2Admin_Run = true
local flying = false
local noclip = false
local InfiniteJump = false
local ESPToggle = false
local CoinFarmToggle = false
local XrayToggle = false
local AimlockToggle = false
local AntiAFKConnection

local FOVSettings = {
    Enabled = false,
@@ -80,6 +81,11 @@
    end
    ClearESP()

    if AntiAFKConnection then
        AntiAFKConnection:Disconnect()
        AntiAFKConnection = nil
    end

    -- Attempt to find and destroy old UI ScreenGuis
    local function findAndDestroy(parent)
        if not parent then return end
@@ -446,302 +452,325 @@
        if Value then XrayOn(workspace) else XrayOff(workspace) end
    end
})
MainTab:CreateToggle({
    Name = "กันหลุด (Anti-AFK)",
    CurrentValue = true,
    Callback = function(Value)
        if Value then
            local bb = game:GetService("VirtualUser")
            AntiAFKConnection = lplayer.Idled:Connect(function()
                bb:CaptureController()
                bb:ClickButton2(Vector2.new())
                game.StarterGui:SetCore("SendNotification", {
                    Title = "MM2 Admin",
                    Text = "ขยับให้แล้วไม่ต้องกลัวหลุด!",
                    Duration = 2
                })
            end)
        else
            if AntiAFKConnection then
                AntiAFKConnection:Disconnect()
                AntiAFKConnection = nil
            end
        end
    end
})
MainTab:CreateButton({
    Name = "ลดแลค bootfps (กระตุกตอนเปิดเล็กน้อย ถึงปานกลาง)",
    Callback = function()
        local removedecals = false
        local g = game
        local w = g.Workspace
        local l = g.Lighting
        local t = w.Terrain
        t.WaterWaveSize = 0
        t.WaterWaveSpeed = 0
        t.WaterReflectance = 0
        t.WaterTransparency = 0
        l.GlobalShadows = false
        l.FogEnd = 9e9
        l.Brightness = 0
        settings().Rendering.QualityLevel = "Level01"
        for i, v in pairs(g:GetDescendants()) do
            if not getgenv().MM2Admin_Run then break end
            if v:IsA("Part") or v:IsA("Union") or v:IsA("CornerWedgePart") or v:IsA("TrussPart") then
                v.Material = "Plastic"
                v.Reflectance = 0
            elseif v:IsA("Decal") or v:IsA("Texture") and removedecals then
                v.Transparency = 1
            elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                v.Lifetime = NumberRange.new(0)
            elseif v:IsA("Explosion") then
                v.BlastPressure = 1
                v.BlastRadius = 1
            elseif v:IsA("Fire") or v:IsA("SpotLight") or v:IsA("Smoke") or v:IsA("Sparkles") then
                v.Enabled = false
            elseif v:IsA("MeshPart") then
                v.Material = "Plastic"
                v.Reflectance = 0
                v.TextureID = 10385902758728957
            end
        end
        for i, e in pairs(l:GetChildren()) do
            if not getgenv().MM2Admin_Run then break end
            if e:IsA("BlurEffect") or e:IsA("SunRaysEffect") or e:IsA("ColorCorrectionEffect") or e:IsA("BloomEffect") or e:IsA("DepthOfFieldEffect") then
                e.Enabled = false
            end
        end
    end
})

-- AIM TAB
local AimSection = AimTab:CreateSection("Aimlock")
AimTab:CreateToggle({
    Name = "เปิดใช้งาน Aimlock (ล็อคเฉพาะมีปืนเท่านั้น)",
    CurrentValue = false,
    Callback = function(Value)
        AimlockToggle = Value
    end
})

local AimFOVSection = AimTab:CreateSection("FOV Settings")
AimTab:CreateToggle({
    Name = "เปิดใช้งาน FOV",
    CurrentValue = false,
    Callback = function(Value)
        FOVSettings.Enabled = Value
    end
})
AimTab:CreateToggle({
    Name = "แสดงวง FOV",
    CurrentValue = false,
    Callback = function(Value)
        FOVSettings.Visible = Value
    end
})
AimTab:CreateSlider({
    Name = "ขนาด FOV",
    Range = {10, 800},
    Increment = 1,
    Suffix = "px",
    CurrentValue = 90,
    Callback = function(Value)
        FOVSettings.Amount = Value
    end
})
-- AimTab:CreateSlider({
--     Name = "ความหนาเส้น",
--     Range = {1, 10},
--     Increment = 1,
--     Suffix = "px",
--     CurrentValue = 1,
--     Callback = function(Value)
--         FOVSettings.Thickness = Value
--     end
-- })
AimTab:CreateSlider({
    Name = "ความโปร่งใส",
    Range = {0, 1},
    Increment = 0.1,
    Suffix = "",
    CurrentValue = 0.5,
    Callback = function(Value)
        FOVSettings.Transparency = Value
    end
})
-- AimTab:CreateToggle({
--     Name = "เปิดโหมดเติมสี",
--     CurrentValue = false,
--     Callback = function(Value)
--         FOVSettings.Filled = Value
--     end
-- })
AimTab:CreateColorPicker({
    Name = "สีวง FOV",
    Color = Color3.fromRGB(255, 255, 255),
    Callback = function(Value)
        FOVSettings.Color = Value
    end
})

-- VISUALS TAB
local ESPSection = VisualsTab:CreateSection("ESP")
VisualsTab:CreateToggle({
    Name = "ESP อัจฉริยะแบบอิงตามบทบาท",
    CurrentValue = false,
    Callback = function(Value)
        ESPToggle = Value
        if Value then
            MakeESP()
        else
            ClearESP()
        end
    end
})
VisualsTab:CreateButton({
    Name = "รีเฟรช ESP",
    Callback = function()
        if ESPToggle then
            ClearESP()
            MakeESP()
        end
    end
})

-- Auto-refresh ESP
task.spawn(function()
    while getgenv().MM2Admin_Run and task.wait(5) do
        if ESPToggle then
            ClearESP()
            MakeESP()
        end
    end
end)

-- TELEPORT TAB
local LocSection = TeleportTab:CreateSection("Locations")
TeleportTab:CreateButton({
    Name = "TP ไปยังล็อบบี้",
    Callback = function()
        if lplayer.Character and lplayer.Character:FindFirstChild("HumanoidRootPart") then
            lplayer.Character.HumanoidRootPart.CFrame = CFrame.new(-108.5, 145, 0.6)
        end
    end
})
TeleportTab:CreateButton({
    Name = "TP ไปยังแผนที่",
    Callback = function()
        for _, v in pairs(workspace:GetChildren()) do
            local spawns = v:FindFirstChild("Spawns")
            local spawnPoint = spawns and spawns:FindFirstChild("Spawn")
            if spawnPoint then
                lplayer.Character.HumanoidRootPart.CFrame = spawnPoint.CFrame
                break
            end
        end
    end
})

local PlySection = TeleportTab:CreateSection("Players")
TeleportTab:CreateInput({
    Name = "TP ไปยังผู้เล่น",
    PlaceholderText = "Player Name",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        for _, v in pairs(plrs:GetPlayers()) do
            if string.find(v.Name:lower(), Text:lower()) then
                if v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    lplayer.Character.HumanoidRootPart.CFrame = v.Character.HumanoidRootPart.CFrame
                end
                break
            end
        end
    end
})
TeleportTab:CreateButton({
    Name = "TP ไปยังฆาตกร",
    Callback = function()
        local Murderer = GetMurderer()
        if Murderer and Murderer.Character and Murderer.Character:FindFirstChild("HumanoidRootPart") then
            lplayer.Character.HumanoidRootPart.CFrame = Murderer.Character.HumanoidRootPart.CFrame
        end
    end
})
TeleportTab:CreateButton({
    Name = "TP ไปยังนายอำเภอ",
    Callback = function()
        local Sheriff = GetSheriff()
        if Sheriff and Sheriff.Character and Sheriff.Character:FindFirstChild("HumanoidRootPart") then
            lplayer.Character.HumanoidRootPart.CFrame = Sheriff.Character.HumanoidRootPart.CFrame
        end
    end
})

-- PLAYER TAB
local MoveSection = PlayerTab:CreateSection("Movement")
PlayerTab:CreateToggle({
    Name = "บิน",
    CurrentValue = false,
    Callback = function(Value)
        if flying ~= Value then ToggleFly() end
    end
})
PlayerTab:CreateKeybind({
    Name = "ปุ่มกดบิน",
    CurrentKeybind = "X",
    HoldToInteract = false,
    Callback = function()
        ToggleFly()
    end
})

PlayerTab:CreateToggle({
    Name = "เดินทะลุ",
    CurrentValue = false,
    Callback = function(Value)
        noclip = Value
    end
})
PlayerTab:CreateKeybind({
    Name = "ปุ่มกดเดินทะลุ",
    CurrentKeybind = "C",
    HoldToInteract = false,
    Callback = function()
        noclip = not noclip
    end
})

PlayerTab:CreateToggle({
    Name = "กระโดดไม่จำกัด",
    CurrentValue = false,
    Callback = function(Value)
        InfiniteJump = Value
    end
})
PlayerTab:CreateKeybind({
    Name = "ปุ่มกดกระโดดไม่จำกัด",
    CurrentKeybind = "V",
    HoldToInteract = false,
    Callback = function()
        InfiniteJump = not InfiniteJump
    end
})

local StatsSection = PlayerTab:CreateSection("Stats")
PlayerTab:CreateSlider({
    Name = "ความเร็วในการเดิน",
    Range = {16, 250},
    Increment = 1,
    Suffix = "px",
    CurrentValue = 16,
    Callback = function(Value)
        if lplayer.Character and lplayer.Character:FindFirstChild("Humanoid") then
            lplayer.Character.Humanoid.WalkSpeed = Value
        end
    end
})
PlayerTab:CreateButton({
    Name = "รีเซ็ตความเร็วในการเดิน",
    Callback = function()
        if lplayer.Character and lplayer.Character:FindFirstChild("Humanoid") then
            lplayer.Character.Humanoid.WalkSpeed = 16
        end
    end
})
PlayerTab:CreateSlider({
    Name = "พลังกระโดด",
    Range = {50, 500},
    Increment = 1,
    Suffix = "px",
    CurrentValue = 50,
    Callback = function(Value)
        if lplayer.Character and lplayer.Character:FindFirstChild("Humanoid") then
            lplayer.Character.Humanoid.JumpPower = Value
        end
    end
})
PlayerTab:CreateButton({
    Name = "รีเซ็ตพลังกระโดด",
    Callback = function()
        if lplayer.Character and lplayer.Character:FindFirstChild("Humanoid") then
            lplayer.Character.Humanoid.JumpPower = 50
        end
    end
})
